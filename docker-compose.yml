version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dubdub_app
    ports:
      - "80:8080"
    volumes:
      - ./logs:/app/logs
    env_file:
      - .env   # Jenkins에서 복사된 .env 파일을 자동 로드
    restart: always