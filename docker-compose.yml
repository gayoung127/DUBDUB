version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dubdub_app
    ports:
      - "80:8080"
    volumes:
      - ./logs:/app/logs
    restart: always

